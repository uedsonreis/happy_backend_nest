import { OrphanageViewInterceptor, OrphanagesViewInterceptor } from './orphanage.interceptor';

describe('OrphanagesViewInterceptor', () => {
    it('should be defined', () => {
        expect(new OrphanagesViewInterceptor()).toBeDefined();
    });
});

describe('OrphanageViewInterceptor', () => {
    it('should be defined', () => {
        expect(new OrphanageViewInterceptor()).toBeDefined();
    });
});